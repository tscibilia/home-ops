# Architecture Decision Records

One file per decision, named `NNNN-slug.md` (`0001-rook-ceph-over-longhorn.md`). Empty until a decision needs its rationale written down.

When superseding an ADR leave the old file in place, flip its Status line, and link forward.
