---
all:
  children:
    truenas:
      hosts:
        truenas.internal:
          ansible_user: sysadmin
          ansible_ssh_private_key_file: ~/.ssh/home-ops
          ansible_python_interpreter: auto_silent
    clonenas:
      hosts:
        clonenas.internal:
          ansible_user: sysadmin
          ansible_ssh_private_key_file: ~/.ssh/home-ops
          ansible_python_interpreter: auto_silent
    vps:
      hosts:
        vps.internal:
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/home-ops
          ansible_python_interpreter: auto_silent
          ansible_port: 22222
    towonel:
      hosts:
        towonel.internal:
          # Temporary DigitalOcean droplet for the towonel migration. Replaced
          # by the permanent VPS at consolidation, then destroyed.
          ansible_host: 157.230.51.94
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
          ansible_python_interpreter: auto_silent
          ansible_port: 22222
