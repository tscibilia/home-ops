---
customization:
  extraKernelArgs:
    - -init_on_alloc # Less security, faster puter
    - -init_on_free # Less security, faster puter
    - -selinux # Less security, faster puter
    - apparmor=0 # Less security, faster puter
    - init_on_alloc=0 # Less security, faster puter
    - init_on_free=0 # Less security, faster puter
    - intel_iommu=on # PCI Passthrough
    - iommu=pt # PCI Passthrough
    - pcie_aspm=off # Disable PCIe ASPM
    - mitigations=off # Less security, faster puter
    - security=none # Less security, faster puter
    - talos.auditd.disabled=1 # Less security, faster puter
    {% if ENV.WORKER %}
    - nvidia_drm.modeset=1 # NVIDIA DRM kernel mode setting
    {% endif %}
  systemExtensions:
    officialExtensions:
      - siderolabs/intel-ucode # Intel CPU Microcode
      - siderolabs/mei # Intel Management Engine
      - siderolabs/nfsrahead # NFS Performance
      {% if ENV.CONTROLPLANE %}
      - siderolabs/i915 # Intel GPU
      {% endif %}
      {% if ENV.WORKER %}
      - siderolabs/nonfree-kmod-nvidia # NVIDIA proprietary kernel module
      - siderolabs/nvidia-container-toolkit # NVIDIA container runtime hook
      {% endif %}
