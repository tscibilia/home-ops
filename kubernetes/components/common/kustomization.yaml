---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  # Notification dispatch failed, 401 Bad credentials, remove for now, fix later...
  # - ./alerts
  - ./secrets
  - ./namespace.yaml
