---
apiVersion: batch/v1
kind: Job
metadata:
  name: ${APP}-list-snapshots
  namespace: ${NS}
spec:
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      automountServiceAccountToken: false
      restartPolicy: OnFailure
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: list
          image: docker.io/restic/restic:0.17.3
          args: ["snapshots"]
          envFrom:
            - secretRef:
                name: ${APP}-volsync-secret
          volumeMounts:
            - name: repository
              mountPath: /repository
      volumes:
        - name: repository
          nfs:
            server: clonenas.internal
            path: /mnt/vault/backups/kubernetes/volsync
