---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./alertmanager
  # Removed, hitting GitHub secondary rate limits too often. Only added to flux-system
  # - ./github-status
