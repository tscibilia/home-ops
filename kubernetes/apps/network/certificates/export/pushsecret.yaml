---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/pushsecret_v1alpha1.json
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: ${SECRET_DOMAIN/./-}-production-tls
spec:
  secretStoreRefs:
    - name: akeyless-secret-store
      kind: ClusterSecretStore
  selector:
    secret:
      name: ${SECRET_DOMAIN/./-}-production-tls
  template:
    engineVersion: v2
    data:
      tls.crt: '{{ index . "tls.crt" | b64enc }}'
      tls.key: '{{ index . "tls.key" | b64enc }}'
  data:
    - match:
        secretKey: &key tls.crt
        remoteRef:
          remoteKey: /network/${SECRET_DOMAIN/./-}-production-tls
          property: *key
    - match:
        secretKey: &key tls.key
        remoteRef:
          remoteKey: /network/${SECRET_DOMAIN/./-}-production-tls
          property: *key
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/pushsecret_v1alpha1.json
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: ${CEAPP_DOMAIN/./-}-production-tls
spec:
  secretStoreRefs:
    - name: akeyless-secret-store
      kind: ClusterSecretStore
  selector:
    secret:
      name: ${CEAPP_DOMAIN/./-}-production-tls
  template:
    engineVersion: v2
    data:
      tls.crt: '{{ index . "tls.crt" | b64enc }}'
      tls.key: '{{ index . "tls.key" | b64enc }}'
  data:
    - match:
        secretKey: &key tls.crt
        remoteRef:
          remoteKey: /network/${CEAPP_DOMAIN/./-}-production-tls
          property: *key
    - match:
        secretKey: &key tls.key
        remoteRef:
          remoteKey: /network/${CEAPP_DOMAIN/./-}-production-tls
          property: *key
