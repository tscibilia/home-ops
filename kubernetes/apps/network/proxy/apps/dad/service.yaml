---
# yaml-language-server: $schema=https://github.com/instrumenta/kubernetes-json-schema/raw/refs/heads/master/v1.18.1/service-v1.json
apiVersion: v1
kind: Service
metadata:
  name: &name proxy-dadnas
  labels:
    app: *name
spec:
  type: ExternalName
  externalName: "${DADNAS_IP}"
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
