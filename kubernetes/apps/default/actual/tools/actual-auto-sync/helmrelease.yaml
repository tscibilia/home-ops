---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app actual-auto-sync
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    controllers:
      *app :
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/seriouslag/actual-auto-sync
              tag: v26.2.1.2@sha256:ff4b3f4c71eb4e3146657851e95b5b0d595a19dca63ef91ed88284f4f0bb6026
            env:
              TZ: ${TIMEZONE:-UTC}
              ACTUAL_SERVER_URL: "http://actual.default.svc.cluster.local:5006"
              CRON_SCHEDULE: "0 7 * * *"
              LOG_LEVEL: "info"
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            resources:
              requests:
                cpu: 10m
                memory: 256Mi
              limits:
                memory: 512Mi
    persistence:
      tmp:
        type: emptyDir
