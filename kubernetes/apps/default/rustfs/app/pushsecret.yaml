---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: &name rustfs-oidc-secret
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - kind: ClusterSecretStore
      name: akeyless-secret-store
  selector:
    secret:
      name: *name
  data:
    - match:
        secretKey: RUSTFS_IDENTITY_OPENID_CLIENT_ID
        remoteRef:
          remoteKey: /security/pocket-id-clients
          property: RUSTFS_OPENID_CLIENT_ID
    - match:
        secretKey: RUSTFS_IDENTITY_OPENID_CLIENT_SECRET
        remoteRef:
          remoteKey: /security/pocket-id-clients
          property: RUSTFS_OPENID_CLIENT_SECRET
