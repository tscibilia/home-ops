---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: &name homebox-oidc-secret
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - kind: ClusterSecretStore
      name: akeyless-secret-store
  selector:
    secret:
      name: *name
  data:
    - match:
        secretKey: HBOX_OIDC_CLIENT_ID
        remoteRef:
          remoteKey: /security/pocket-id-clients
          property: HBOX_OIDC_CLIENT_ID
    - match:
        secretKey: HBOX_OIDC_CLIENT_SECRET
        remoteRef:
          remoteKey: /security/pocket-id-clients
          property: HBOX_OIDC_CLIENT_SECRET
