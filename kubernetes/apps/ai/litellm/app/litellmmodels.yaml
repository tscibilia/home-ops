---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name qwen-local
spec:
  # An open-weight dense model capable of vision and language tasks.
  # Theoretically close to Sonnet 5, however local quants reduce its ability.
  modelName: *name
  params:
    model: openai/Qwen3.8-27B
    # Router, not the service: naming the model is what wakes it when comfyui
    # holds the slot. The backend's displayName keeps `model` unchanged.
    apiBase: http://ai3090-router-router-proxy.ai.svc.cluster.local:8080/v1
    apiKeyRef:
      name: litellm-secret
      key: LLAMA_API_KEY
    additional:
      # Keys under `additional` are copied into litellm_params VERBATIM
      # expects snake_case, operator only camelCases its own typed fields
      max_parallel_requests: 1
  info:
    mode: chat
    maxTokens: 32768
    maxInputTokens: 147456 # contextSize 180224 minus maxOutputTokens; llama.cpp counts both against one pool
    maxOutputTokens: 32768
    supportsFunctionCalling: true
    supportsVision: true
    extra:
      supports_reasoning: true
      # CoreWeave via OpenRouter: $0.40 in / $3.00 out / $0.15 cached read only
      input_cost_per_token: 0.0000000120
      cache_creation_input_token_cost: 0.0000000120
      cache_read_input_token_cost: 0.0000000010
      output_cost_per_token: 0.0000000500
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name qwen-local-fast
spec:
  # Same llama-qwen as qwen-local — model-picker change, not a restart.
  # Only difference: thinking off plus the model's instruct sampler row.
  modelName: *name
  params:
    model: openai/Qwen3.8-27B
    # Router, not the service: naming the model is what wakes it when comfyui
    # holds the slot. The backend's displayName keeps `model` unchanged.
    apiBase: http://ai3090-router-router-proxy.ai.svc.cluster.local:8080/v1
    apiKeyRef:
      name: litellm-secret
      key: LLAMA_API_KEY
    additional:
      # Keys under `additional` are copied into litellm_params VERBATIM
      # expects snake_case, operator only camelCases its own typed fields
      max_parallel_requests: 1
      extra_body:
        # extra_body bypasses the proxy's drop_params, so this reaches llama.cpp intact
        chat_template_kwargs:
          enable_thinking: false
        temperature: 0.7
        top_p: 0.80
        presence_penalty: 1.5
  info:
    mode: chat
    maxTokens: 32768
    maxInputTokens: 147456 # contextSize 180224 minus maxOutputTokens; llama.cpp counts both against one pool
    maxOutputTokens: 32768
    supportsFunctionCalling: true
    supportsVision: true
    extra:
      supports_reasoning: false
      input_cost_per_token: 0.0000000120
      cache_creation_input_token_cost: 0.0000000120
      cache_read_input_token_cost: 0.0000000010
      output_cost_per_token: 0.0000000500
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name comfyui
spec:
  # Not a chat model — the wake switch for the GPU slot. Sending it
  # anything swaps ComfyUI in; the reply carries the UI's URL.
  modelName: *name
  params:
    model: openai/comfyui
    apiBase: http://ai3090-router-router-proxy.ai.svc.cluster.local:8080/v1
    apiKeyRef:
      name: litellm-secret
      key: LLAMA_API_KEY
    additional:
      max_parallel_requests: 1
      # Must outlast a cold start: if the client gives up first the activator
      # scales ComfyUI back to zero and the wake is lost.
      timeout: 1800
  info:
    mode: chat
    maxTokens: 512
    maxInputTokens: 512
    maxOutputTokens: 512
    extra:
      input_cost_per_token: 0.0
      output_cost_per_token: 0.0
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name memini-embed
spec:
  modelName: *name
  params:
    model: openai/memini-embed
    apiBase: http://memini-embed.ai.svc.cluster.local:8080/v1
    apiKeyRef:
      name: litellm-secret
      key: MEMINI_EMBED_API_KEY
  info:
    mode: embedding
    extra:
      input_cost_per_token: 0.0000000120
      output_cost_per_token: 0.0000000500
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name memini-rerank
spec:
  modelName: *name
  params:
    model: infinity/memini-rerank
    apiBase: http://memini-rerank.ai.svc.cluster.local:8080/v1
    apiKeyRef:
      name: litellm-secret
      key: MEMINI_RERANK_API_KEY
  info:
    mode: rerank
    extra:
      input_cost_per_token: 0.0000000120
      output_cost_per_token: 0.0000000500
# deepseek-v4-flash-free removed from OpenCode Zen ~08/21/26
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name deepseek-v4-flash
spec:
  # Sparse MoE model that activates 13B on output from 284B parameter backbone. This is a
  # re-post-trained revision released 2026-07-31, and rumored to be on par with Sonnet 5.
  modelName: *name
  params:
    model: openai/deepseek/deepseek-v4-flash
    apiBase: https://openrouter.ai/api/v1
    apiKeyRef:
      name: litellm-secret
      key: OPENROUTER_API_KEY
    additional:
      extra_body:
        provider:
          # Zen ~3x this output cost and adds peak/off-peak pricing.
          order: ["deepinfra"]
          # DeepInfra's shared pool 429s under load; fall back, but only to hosts
          # at or below its rate. USD per 1M tokens, matched to info.extra below.
          allow_fallbacks: true
          max_price:
            prompt: 0.10
            completion: 0.20
  info:
    mode: chat
    maxTokens: 131072
    maxInputTokens: 1048576
    maxOutputTokens: 131072
    supportsFunctionCalling: true
    supportsPromptCaching: true
    extra:
      supports_reasoning: true
      # DeepInfra via OpenRouter: $0.09 in / $0.18 out / $0.016 cached read only
      input_cost_per_token: 0.0000000900
      cache_read_input_token_cost: 0.0000000160
      output_cost_per_token: 0.0000001800
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name deepseek-v4.1-flash
spec:
  # Sparse MoE model that activates 8B parameters on input & 16B on output from 522B parameter backbone.
  # Asymetric split, keeps per-token cost low. Released 2026-09-10, and rumored to be on par with Opus 5.
  modelName: *name
  params:
    model: openai/deepseek/deepseek-v4.1-flash
    apiBase: https://openrouter.ai/api/v1
    apiKeyRef:
      name: litellm-secret
      key: OPENROUTER_API_KEY
    additional:
      extra_body:
        provider:
          order: ["morph", "deepinfra"]
          # DeepInfra's shared pool 429s under load; fall back, but only to hosts
          # at or below its rate. USD per 1M tokens, matched to info.extra below.
          allow_fallbacks: true
          max_price:
            prompt: 0.25
            completion: 0.70
  info:
    mode: chat
    maxTokens: 131072
    maxInputTokens: 1048576
    maxOutputTokens: 131072
    supportsFunctionCalling: true
    supportsPromptCaching: true
    supportsVision: true
    extra:
      supports_reasoning: true
      # DeepInfra via OpenRouter: $0.20 in / $0.60 out / $0.03 cached read only
      input_cost_per_token: 0.0000002000
      cache_read_input_token_cost: 0.0000000300
      output_cost_per_token: 0.0000006000
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name kimi-k3
spec:
  modelName: *name
  params:
    model: openai/kimi-k3
    # https://opencode.ai/docs/zen#pricing
    apiBase: https://opencode.ai/zen/v1
    apiKeyRef:
      name: litellm-secret
      key: OPENCODE_API_KEY
  info:
    mode: chat
    maxTokens: 131072
    maxInputTokens: 1048560
    maxOutputTokens: 131072
    supportsFunctionCalling: true
    supportsPromptCaching: true
    supportsVision: true
    extra:
      # Zen: $3.00 in / $15.00 out / $0.30 cached read; no cached write tier
      input_cost_per_token: 0.0000030000
      cache_read_input_token_cost: 0.0000003000
      output_cost_per_token: 0.0000150000
      supports_reasoning: true
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name laguna-s-2-1
spec:
  modelName: *name
  params:
    model: openai/poolside/laguna-s-2.1
    apiBase: https://inference.poolside.ai/v1
    apiKeyRef:
      name: litellm-secret
      key: POOLSIDE_API_KEY
  info:
    mode: chat
    maxTokens: 32768
    maxInputTokens: 1048576
    maxOutputTokens: 32768
    supportsFunctionCalling: true
    supportsPromptCaching: true
    extra:
      input_cost_per_token: 0.0000000120
      cache_creation_input_token_cost: 0.0000000120
      cache_read_input_token_cost: 0.0000000010
      output_cost_per_token: 0.0000000500
      supports_reasoning: true
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name qwen-image-3
spec:
  modelName: *name
  params:
    # OpenRouter serves image models over /chat/completions, not /images/generations,
    # so mode stays `chat` — the picture comes back in message.images[].
    model: openai/qwen/qwen-image-3
    apiBase: https://openrouter.ai/api/v1
    apiKeyRef:
      name: litellm-secret
      key: OPENROUTER_API_KEY
  info:
    mode: chat
    maxInputTokens: 65536
    supportsVision: true
    extra:
      # txt+img -> img: emits only images; $0.003 in / $0.03 out per image (fixed img cost)
      input_cost_per_token: 0.0
      output_cost_per_token: 0.0
      input_cost_per_image: 0.003
      output_cost_per_image: 0.03
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmmodel_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMModel
metadata:
  name: &name nano-banana-2-lite
spec:
  modelName: *name
  params:
    model: openai/google/gemini-3.1-flash-lite-image
    apiBase: https://openrouter.ai/api/v1
    apiKeyRef:
      name: litellm-secret
      key: OPENROUTER_API_KEY
  info:
    mode: chat
    maxTokens: 66000
    maxInputTokens: 65536
    maxOutputTokens: 66000
    supportsVision: true
    extra:
      # txt+img -> txt+img: $0.25 in / $1.50 out per 1M text tokens (avg 1K img = $0.034)
      input_cost_per_token: 0.0000002500
      output_cost_per_token: 0.0000015000
      output_cost_per_image_token: 0.0000300000
