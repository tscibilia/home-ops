---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: &name guacamole-oidc-secret
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - kind: ClusterSecretStore
      name: akeyless-secret-store
  selector:
    secret:
      name: *name
  data:
    - match:
        secretKey: OPENID_CLIENT_ID
        remoteRef:
          remoteKey: /security/pocket-id-clients
          property: GUAC_OIDC_CLIENT_ID
